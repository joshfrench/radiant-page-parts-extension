-# A fuller implementation would display an appropriate thumbnail for the
-# current file rather than simply the filename.

- if page_part.document.file?
	%div.row
		== Delete #{page_part.document.original_filename}?
		= check_box_tag "page[parts_attributes][#{page_part_counter}][delete]", :value => true
%div.row
	= file_field_tag "page[parts_attributes][#{page_part_counter}][document]", :value => page_part.document.original_filename